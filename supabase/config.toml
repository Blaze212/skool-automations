# Minimal local Supabase config — only the parts needed for our schema.
# Most settings keep their defaults from `supabase init`.

project_id = "skool-automations"

[api]
# Offset from CareerSystems (54321) to avoid port conflicts when both stacks run locally.
port = 54331
enabled = true
# Expose the internal_cs schema so PostgREST routes /rest/v1 requests for it.
schemas = ["public", "graphql_public", "internal_cs"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54332
shadow_port = 54330
major_version = 15

[studio]
port = 54333

[inbucket]
port = 54334

[analytics]
port = 54337

[functions.fractional-onboarding-form-webhook]
verify_jwt = false
